server.port=1900
spring.datasource.url=jdbc:mysql://localhost:3306/MP

spring.datasource.username=root
spring.datasource.password=tiger

#The SQL dialect makes hibernate generate appropriate SQL statements for the chosen database.
#spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL5InnoDBDialect

spring.jpa.hibernate.ddl-auto=update 
#updates the table whenever some modifications are being done to the table.

#Used for logging(debugging) purpose so that we can easily debug our application and trace the errors.
logging.level.org.hibernate.SQL=DEBUG
logging.level.org.hibernate.type=TRACE